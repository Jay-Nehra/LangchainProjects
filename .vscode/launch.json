{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ReachOut Terminal Runner",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/reachOut.py",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env",
            "cwd": "${workspaceFolder}"
        },
    ]
}
